import React from 'react';
import './RightSidebar.css'; // RightSidebar 전용 CSS

function RightSidebar() {
    return (
        <div className="right-sidebar-container">
            {/* 이 곳은 현재 비어있습니다. */}
            {/* 필요한 경우 여기에 광고, 추천 등 내용을 추가할 수 있습니다. */}
        </div>
    );
}

export default RightSidebar;